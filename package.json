{
  "name": "visual-test-app",
  "version": "1.0.0",
  "private": true,
  "description": "Static test site that exercises the SnapDiff GitHub Action and Playwright reporter end-to-end.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.0",
    "@snapdiff/playwright": "file:./vendor/snapdiff-playwright-0.1.2.tgz"
  }
}
